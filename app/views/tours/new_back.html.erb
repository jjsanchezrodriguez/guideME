<div> <%= @user.name %> </div>
	
<%= form_for([@tourist, @tour], method: "post") do |f|%>
    
    <div class="name-container">
        <%= f.label("Excursions") %>
        <%= f.collection_select(:excursion_id, Offer.all, :excursion_name, :id) %>
    </div>     
    <%= submit_tag("Create Tour!!!") %>
<% end %>

<%= link_to "Back", @guide%> 

